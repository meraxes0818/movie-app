import Search from "@/components/main/Search";
import React, { Suspense } from "react";

const SearchPage = () => {
  return (
    <Suspense>
      <Search />
    </Suspense>
  );
};

export default SearchPage;
